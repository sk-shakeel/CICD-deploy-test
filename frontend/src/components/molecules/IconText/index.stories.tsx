import React from 'react'
import { Meta } from '@storybook/react'

import IconText from '.'

export default {
  title: 'Molecules/IconText',
  component: IconText,
} as Meta

export const Basic = () => <IconText />
